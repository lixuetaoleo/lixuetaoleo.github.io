.article-body{
  display: flex;
  max-width: 100vw;
  min-height: 100vh;
  margin: 0;
  overflow-x: hidden;
  flex-direction: column;
  align-items: center;
}

.articlearticle{
  margin:0 auto auto auto;
}

.title, .article-meta{
  text-align: center;
}

.article-content{
  width: 720px;
}

code{
  font-family: 'Ubuntu Mono', monospace;
  font-weight:bold;
}
.code{
  font-family: 'Ubuntu Mono', monospace;
  font-weight:bold;
}

.article-content li{
  margin-top:10px;
}